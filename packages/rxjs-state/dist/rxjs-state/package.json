{
  "name": "rxjs-state",
  "version": "0.0.0-alpha.0",
  "scripts": {
    "prebuild": "",
    "build": "npm run compile",
    "clean": "rimraf dist/**/*",
    "link": "cd ./dist/rxjs-state && npm link",
    "compile": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "rxjs": "^6.5.3"
  },
  "devDependencies": {
    "rimraf": "^3.0.1",
    "typescript": "^3.7.3"
  }
}
